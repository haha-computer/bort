# fly.toml app configuration file generated for bort on 2026-01-20T01:55:00Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'bort'
primary_region = 'iad'

[build]

# No HTTP service - this is a worker process
[processes]
  app = "node bot.js"

[[vm]]
  size = 'shared-cpu-1x'
  memory = '256mb'
  auto_stop_machines = false
